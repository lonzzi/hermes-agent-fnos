#!/bin/bash
set -eu
# Docker resources are validated before install_callback runs.
# Credentials must already have been created by install_init.
[ -s "$TRIM_PKGETC/dashboard.env" ] || { echo 'Dashboard credentials were not initialized' > "$TRIM_TEMP_LOGFILE"; exit 1; }
mkdir -p "$TRIM_PKGVAR/hermes"
