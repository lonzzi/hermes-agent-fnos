#!/bin/bash
case "${1:-}" in
  start|stop) exit 0 ;;
  status)
    state=$(docker inspect --format '{{.State.Status}} {{if .State.Health}}{{.State.Health.Status}}{{end}}' hermes-agent-fnos 2>/dev/null) || exit 3
    [ "$state" = 'running healthy' ] && exit 0
    exit 3 ;;
  *) exit 1 ;;
esac
